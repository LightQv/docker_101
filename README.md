# Getting Started with Docker_101

Run the following command to build and start the Docker containers :

`npm
docker compose -f docker-compose.dev.yml up --build
`

NB :
Add **_-d_** at the end of the above command to detach the Docker containers inside Docker Desktop App.
